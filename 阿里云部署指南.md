# 🚀 阿里云 ECS 部署指南

**适用：** Golf AI Tracker  
**版本：** v1.0.0-stable  
**日期：** 2026-03-12

---

## 📋 前置要求

### 阿里云资源
- [x] ECS 实例（Ubuntu 20.04+）
- [x] 公网 IP
- [x] 安全组开放端口：80, 443, 3000

### 域名
- [x] ai-golf.top（已备案）

---

## 🔧 1. 安装 Docker

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 启动 Docker
sudo systemctl enable docker
sudo systemctl start docker

# 安装 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 验证安装
docker --version
docker-compose --version
```

---

## 📦 2. 部署项目

### 方式 A：从 GitHub 拉取

```bash
# 克隆项目
cd /var/www
git clone git@github.com:erste-zhou/ai-golf.top.git
cd ai-golf.top

# 创建环境变量文件
cp server/.env.example server/.env
nano server/.env  # 编辑配置
```

### 方式 B：本地上传

```bash
# 本地打包
cd ~/Desktop/ai-golf
tar -czf ai-golf.tar.gz .

# 上传到服务器
scp ai-golf.tar.gz root@你的 ECS IP:/var/www/

# 服务器解压
ssh root@你的 ECS IP
cd /var/www
tar -xzf ai-golf.tar.gz
```

---

## ⚙️ 3. 配置环境变量

### 编辑 server/.env

```bash
nano /var/www/ai-golf.top/server/.env
```

**填写以下内容：**

```env
# 服务端口
PORT=3000

# MongoDB 连接
MONGO_URI=mongodb+srv://golfcoach:ibm00ibm@cluster0.omomlp0.mongodb.net/golf-tracker?retryWrites=true&w=majority&appName=Cluster0

# DeepSeek API（已配置）
DEEPSEEK_API_KEY=sk-35eb6fa57dff414d9597cdd91c77f250

# 天气 API（已配置）
WEATHER_API_KEY=933a528d7e1147ed97744718251712

# JWT 密钥
JWT_SECRET=jwt_secret_2026_golf_tracker_ai_golf_top_secure_key_32chars

# 邮件服务（可选）
EMAIL_HOST=smtp.139.com
EMAIL_PORT=25
EMAIL_USER=13928737359@139.com
EMAIL_PASS=5c5252c9e8858fe22500
EMAIL_SECURE=false
```

### 编辑前端环境变量

```bash
nano /var/www/ai-golf.top/client/.env.production
```

**填写：**

```env
VITE_WEATHER_API_KEY=933a528d7e1147ed97744718251712
VITE_API_URL=https://api.ai-golf.top
```

---

## 🐳 4. 构建并启动

```bash
cd /var/www/ai-golf.top

# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f
```

---

## 🌐 5. 配置 Nginx 反向代理

### 安装 Nginx

```bash
sudo apt install nginx -y
```

### 配置反向代理

```bash
sudo nano /etc/nginx/sites-available/ai-golf.top
```

**填写以下内容：**

```nginx
server {
    listen 80;
    server_name ai-golf.top www.ai-golf.top;

    # 前端
    location / {
        proxy_pass http://localhost:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 后端 API
    location /api {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 启用配置

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/ai-golf.top /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

---

## 🔒 6. 配置 HTTPS（可选但推荐）

### 使用 Let's Encrypt

```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx -y

# 获取证书
sudo certbot --nginx -d ai-golf.top -d www.ai-golf.top

# 自动续期
sudo crontab -e
# 添加：0 3 * * * certbot renew --quiet
```

---

## 🔧 7. 配置阿里云安全组

**访问：** 阿里云控制台 → ECS → 安全组

**添加入站规则：**

| 端口 | 协议 | 授权对象 | 说明 |
|------|------|----------|------|
| 80 | TCP | 0.0.0.0/0 | HTTP |
| 443 | TCP | 0.0.0.0/0 | HTTPS |
| 22 | TCP | 你的 IP | SSH |

---

## ✅ 8. 验证部署

### 检查服务状态

```bash
# 查看 Docker 容器
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 访问测试

1. **前端：** http://ai-golf.top
2. **后端 API：** http://ai-golf.top:3000/login
3. **测试功能：**
   - [ ] 注册/登录
   - [ ] 添加成绩
   - [ ] 查看历史记录
   - [ ] AI 教练

---

## 🔄 9. 更新部署

```bash
cd /var/www/ai-golf.top

# 拉取最新代码
git pull origin main

# 重新构建并重启
docker-compose down
docker-compose build
docker-compose up -d
```

---

## 📊 常用命令

### Docker 相关
```bash
# 查看容器状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 重启服务
docker-compose restart

# 停止服务
docker-compose down

# 启动服务
docker-compose up -d
```

### Nginx 相关
```bash
# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx

# 查看状态
sudo systemctl status nginx
```

---

## 🆘 故障排查

### 问题 1：无法访问网站

**检查：**
```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 检查 Docker 容器
docker-compose ps

# 检查端口占用
sudo netstat -tlnp | grep :80
```

### 问题 2：后端连接失败

**检查：**
```bash
# 查看后端日志
docker-compose logs server

# 检查 MongoDB 连接
docker-compose exec server node -e "console.log(process.env.MONGO_URI)"
```

### 问题 3：前端无法连接后端

**检查：**
```bash
# 确认 VITE_API_URL 配置
cat client/.env.production

# 重新构建前端
docker-compose build client
docker-compose up -d client
```

---

## 📞 支持

- **GitHub:** https://github.com/erste-zhou/ai-golf.top
- **域名:** ai-golf.top
- **部署方式：** 阿里云 ECS + Docker Compose

---

**部署完成！** 🎉
