# 🚀 阿里云一键部署

**服务器信息：**
- **IP:** 121.43.61.10
- **实例 ID:** iZbp1iiwv72rzmfgcs1a7sZ
- **系统:** Alibaba Cloud Linux 3.2104 LTS
- **配置:** 2 核 2GB 3Mbps

---

## 📋 部署前准备

### 1. 配置 SSH 密钥（可选但推荐）

```bash
# 生成 SSH 密钥（如果没有）
ssh-keygen -t ed25519 -C "ai-golf-deploy"

# 复制公钥到服务器
ssh-copy-id root@121.43.61.10
```

### 2. 配置安全组

**阿里云控制台 → ECS → 安全组**

添加入站规则：

| 端口 | 协议 | 授权对象 | 说明 |
|------|------|----------|------|
| 22 | TCP | 0.0.0.0/0 | SSH |
| 80 | TCP | 0.0.0.0/0 | HTTP |
| 443 | TCP | 0.0.0.0/0 | HTTPS |
| 3000 | TCP | 0.0.0.0/0 | 后端 API |
| 8001 | TCP | 0.0.0.0/0 | 前端 |

---

## 🎯 一键部署

### 方式 A：使用部署脚本（推荐）

```bash
cd ~/Desktop/ai-golf
./deploy-to-aliyun.sh
```

脚本会自动：
1. ✅ 检查 SSH 连接
2. ✅ 上传代码
3. ✅ 安装 Docker
4. ✅ 安装 Docker Compose
5. ✅ 配置环境变量
6. ✅ 构建并启动服务

### 方式 B：手动部署

#### 1. 连接服务器

```bash
ssh root@121.43.61.10
```

#### 2. 安装 Docker

```bash
curl -fsSL https://get.docker.com | sh
systemctl enable docker
systemctl start docker
```

#### 3. 安装 Docker Compose

```bash
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" \
    -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```

#### 4. 上传代码

```bash
# 方式 1：Git 克隆
cd /var/www
git clone git@github.com:erste-zhou/ai-golf.top.git
cd ai-golf.top

# 方式 2：本地上传（在本地执行）
# scp -r ~/Desktop/ai-golf/* root@121.43.61.10:/var/www/ai-golf.top/
```

#### 5. 配置环境变量

```bash
cd /var/www/ai-golf.top

# 后端环境变量
cat > server/.env << 'EOF'
PORT=3000
MONGO_URI=mongodb+srv://golfcoach:ibm00ibm@cluster0.omomlp0.mongodb.net/golf-tracker?retryWrites=true&w=majority&appName=Cluster0
DEEPSEEK_API_KEY=sk-35eb6fa57dff414d9597cdd91c77f250
WEATHER_API_KEY=933a528d7e1147ed97744718251712
JWT_SECRET=jwt_secret_2026_golf_tracker_ai_golf_top_secure_key_32chars
EMAIL_HOST=smtp.139.com
EMAIL_PORT=25
EMAIL_USER=13928737359@139.com
EMAIL_PASS=5c5252c9e8858fe22500
EMAIL_SECURE=false
EOF

# 前端环境变量
cat > client/.env.production << 'EOF'
VITE_WEATHER_API_KEY=933a528d7e1147ed97744718251712
VITE_API_URL=https://api.ai-golf.top
EOF
```

#### 6. 构建并启动

```bash
cd /var/www/ai-golf.top

# 安装依赖
cd server && npm install --production && cd ..
cd client && npm install && npm run build && cd ..

# Docker 部署
docker-compose build
docker-compose up -d

# 查看状态
docker-compose ps
```

---

## ✅ 访问测试

### 直接访问 IP

- **前端:** http://121.43.61.10:8001
- **后端:** http://121.43.61.10:3000

### 测试项目

- [ ] 访问前端页面
- [ ] 用户注册
- [ ] 用户登录
- [ ] 添加成绩
- [ ] 查看历史记录
- [ ] AI 教练对话

---

## 🔧 常用命令

### 查看服务状态

```bash
# SSH 登录服务器
ssh root@121.43.61.10

# 查看容器状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 重启服务

```bash
docker-compose restart
```

### 更新部署

```bash
cd /var/www/ai-golf.top

# 拉取最新代码
git pull origin main

# 重新构建
docker-compose down
docker-compose build
docker-compose up -d
```

### 查看日志

```bash
# 全部日志
docker-compose logs -f

# 只看后端
docker-compose logs -f server

# 只看前端
docker-compose logs -f client
```

---

## 🌐 配置域名（可选）

### 1. DNS 解析

在域名服务商添加 A 记录：

| 主机记录 | 记录类型 | 记录值 |
|----------|----------|--------|
| @ | A | 121.43.61.10 |
| www | A | 121.43.61.10 |
| api | A | 121.43.61.10 |

### 2. Nginx 反向代理

```bash
# 安装 Nginx
yum install nginx -y  # Alibaba Cloud Linux 使用 yum

# 配置
cat > /etc/nginx/conf.d/ai-golf.top.conf << 'EOF'
server {
    listen 80;
    server_name ai-golf.top www.ai-golf.top;

    location / {
        proxy_pass http://localhost:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF

# 启动 Nginx
systemctl enable nginx
systemctl start nginx
```

---

## 🆘 故障排查

### 问题 1：无法 SSH 连接

```bash
# 检查安全组是否开放 22 端口
# 检查服务器是否运行中
# 检查 SSH 密钥权限
chmod 600 ~/.ssh/id_ed25519
```

### 问题 2：Docker 容器启动失败

```bash
# 查看日志
docker-compose logs

# 检查端口占用
netstat -tlnp | grep :3000
netstat -tlnp | grep :8001
```

### 问题 3：前端无法连接后端

```bash
# 检查环境变量
cat client/.env.production

# 重新构建前端
docker-compose build client
docker-compose up -d client
```

---

## 📊 服务器监控

### 查看资源使用

```bash
# CPU 和内存
top

# 磁盘空间
df -h

# Docker 资源
docker stats
```

### 设置监控告警

**阿里云控制台 → 云监控**

设置 CPU、内存、磁盘使用率告警。

---

## 📞 支持

- **GitHub:** https://github.com/erste-zhou/ai-golf.top
- **服务器 IP:** 121.43.61.10
- **部署脚本:** deploy-to-aliyun.sh

---

**部署完成！🎉**
